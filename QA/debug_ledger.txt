v2 run 1: replay isolated-objects assertion failed after pickup, recording, save/load and pose checks passed. Run 2 (same test plus component inventory) reproduced: Unity automatically attaches four UniversalAdditionalLightData components. No physics, collider, or game MonoBehaviour found. Allowed only URP additional light/camera presentation components in assertion; retained exclusion of all game scripts and physics.
v2 run 3: rendering-only replay checks and non-mutating scrub passed. Deliberate corrupt-file test exposed InvalidDataException escaping LoadTake filter: it derives from SystemException, not IOException. Add explicit invalid-data handling at shared file reader; preserve current valid take.
Lens-wall regression reproduced: head z=-1.12, view z=-0.88, held lens z=-0.21 while a wall begins about z=-0.90. Neither first-person offset nor held lens had a collision query; detached-head/third-person queries do not cover this path. Added view linecast and held-lens spherecast after camera update, before recording.
Final complete Play Mode run PASS: corrupt-file handling, storage retry, replay isolation, pickup obstruction and both camera-wall regressions all pass.
Blender-to-Unity surface audit: isolated rest-mesh sleeve wall normals, 672 Left faces inward versus 672 Right faces outward. Reflection from radial profile to Left-arm coordinates has determinant -1, while the Right-arm map has determinant +1. Correct winding with Blender bmesh recalculation before subdivision, for cuffs and rounded panels too. Generator now asserts both sleeve walls point outward.

Hospital navigation: build/probe both failed first ward. Probe floor bounds were vertical (Y extent 2m; Z extent .09m). Prop() replaced FBX root rotation and erased Blender axis conversion. Fixed shared Prop() to preserve imported transform under a placement parent. Follow-up build verifies floor bounds and all six room paths.

Hospital take reload: the binary structure, rotations and timestamps validated independently. Fresh-process reproduction reported Footstep sample=3528 length=3528. Unity exposes an end-of-clip cursor equal to sample count. Recorder now clamps captured cursors; loader accepts that valid EOF boundary and normalizes it before seeking. Values beyond EOF remain rejected.

Review transition: playtest_04 reached extraction, save and house review. Log shows Hospital loaded and immediately House loaded again, with no close action. RunSession.Update treated old house take as a finished review while LoadSceneAsync was pending. Add explicit review-loading guard until the new scene binds and playback starts. Test stopped at the known idle stage 10; no pass claimed.
AI sight probe: LastKnown jumped from (0,.03,8) to hidden root (-5,.08,-3) while the interpolated head still represented the previous pose. Store the same observed head XZ used for LOS, not the independently advanced root.

Forced door navigation: RunDoor reproduced an open, physically clear doorway with a still-partial agent path. Probe: current=PathPartial, fresh=PathComplete, destination had collapsed to reachable endpoint (-5.5,.04,10.59). Surgeon now retains its requested destination separately and door changes schedule a replan after the carving update. Applies to player-opened and AI-opened doors. Added an assertion that the actor actually crosses into the next room.
